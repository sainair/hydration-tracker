name: prevent-shut
on:
    schedule:
        - cron: "*/5 * * * *"
    workflow_dispatch:

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - run: curl -sF "https://glassapi.onrender.com" || true